{
  "$schema": "https://turborepo.org/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", "lib/**"]
    },
    "test": {
      "outputs": ["test-results/**", "playwright-report/**"]
    },
    "dev": {
      "cache": false
    }
  }
}

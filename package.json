{
  "name": "@eventpop/frame",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@changesets/cli": "^2.25.0",
    "@playwright/test": "^1.26.1",
    "prettier": "^2.7.1",
    "turbo": "^1.5.5"
  },
  "scripts": {
    "publish": "turbo build && changeset publish"
  }
}
